export interface Item {
    id: string
    name: string
    emoji: string
    description: string
    worth?: number
    role?: number
    aliases?: Array<string>
    speed?: number
    boobies?: string
}