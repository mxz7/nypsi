export enum PunishmentType {
    MUTE = "mute",
    BAN = "ban",
    UNMUTE = "unmute",
    WARN = "warn",
    KICK = "kick",
    UNBAN = "unban",
    FILTER_VIOLATION = "filter violation",
}

export enum LogType {
    MEMBER = "member",
    MESSAGE = "message",
    CHANNEL = "channel",
    ROLE = "role",
    SERVER = "server",
    EMOJI = "emoji",
}
